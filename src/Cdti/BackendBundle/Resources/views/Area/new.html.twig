{% extends '::backend.html.twig' %}

{% block id 'area_new' %}
{% block title %}Añadir una nueva area{% endblock %}

{% block content %}
{{ form_errors(form) }}
<form action="{{ path('backend_area_create') }}" method="post" {{ form_enctype(form) }}>
    <div class="input_wrapper">
    {{ form_row(form.nombre) }}
    </div>
    <p>
        <button class="button_primary button continue" type="submit">Añadir área</button>
    </p>
    {{ form_rest(form) }}
</form>
{% endblock %}
{% block button_area %}
<a href="{{ path('backend_area') }}" class="button"><div class="icon back"></div>Volver al listado</a>
{% endblock %}
