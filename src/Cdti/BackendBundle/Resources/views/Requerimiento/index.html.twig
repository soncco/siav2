{% extends '::backend.html.twig' %}

{% block id 'requerimiento' %}
{% block title %}Listado de requerimientos{% endblock %}

{% block content %}
<table>
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <th>{{ entity.nombre }}</th>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_req_edit', { 'id': entity.id }) }}">Modificar</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
{% block button_area %}
<a class="button_primary button continue" href="{{ path('backend_req_new') }}">
  Añadir un nuevo requerimiento
</a>
{% endblock %}